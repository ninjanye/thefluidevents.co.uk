extends layout

block content
  .hero
    img(src="/img/logo_wide_light.svg" alt="The Fluid Events Company Logo")

  include _callouts

  a#about(visibility="hidden")
  include _about

  include _promo

  a#packages(visibility="hidden")
  include _packages

  a#contact(visibility="hidden")
  include _contact